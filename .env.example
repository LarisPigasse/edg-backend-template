# =============================================================================
# EDG Backend Template - Environment Variables
# =============================================================================

# Service Configuration
SERVICE_NAME=EDG Backend Template
PORT=3001
NODE_ENV=development

# Database Configuration (REQUIRED)
DB_NAME=nome_database
DB_USER=utente_database
DB_PASSWORD=password_database
DB_HOST=hostname_ip_database 
DB_PORT=3306

# Database Sync (Development only)
# DB_SYNC=true  # Uncomment to sync database tables on startup

# Security Configuration
RATE_LIMIT_WINDOW=16
RATE_LIMIT_MAX_ATTEMPTS=128

# CORS Origins (comma separated - aggiungi i tuoi domini)
CORS_ORIGINS=https://your-frontend.com,https://your-app.com

# EdgLogger Integration (Optional)
# LOGGER_API_URL=http://localhost:4000
# LOGGER_API_KEY=your-logger-api-key

# =============================================================================
# Notes:
# 1. Copia questo file come .env e modifica i valori
# 2. Non committare mai il file .env (è già in .gitignore)
# 3. DB_SYNC=true forza la sincronizzazione delle tabelle al startup
# 4. In production usa sempre DB_SYNC=false o rimuovilo completamente
# =============================================================================